24.1.13 Step 13

Based on the output of the previous command, you can now use the rpm command to determine which package provided the /usr/sbin/edquota file. Execute the following command:

rpm -q -f /usr/sbin/edquota

[root@localhost /]# rpm -q -f /usr/sbin/edquota
quota-4.01-17.el7.x86_64

